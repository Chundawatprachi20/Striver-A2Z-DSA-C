class Solution {
public:

    int pascalTriangleI(int r, int c)
    {
        int ans = 1;

        r = r - 1;
        c = c - 1;

        for(int i = 0; i < c; i++)
        {
            ans = ans * (r - i);
            ans = ans / (i + 1);
        }

        return ans;
    }
};
